<h1>Users</h1>
<% @users.each do |user| %>
  <div class="card mb-3">
    <div class="card-body">
      <h2 class="card-title"><%= link_to user.username, user_path(user) %></h2>
      <p class="card-text">Email: <%= user.email %></p>
    </div>
  </div>
<% end %>